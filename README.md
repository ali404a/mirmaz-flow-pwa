# mirmaz FLOW — نظام إدارة الإنتاج التسويقي والإبداعي

نظام متكامل لإدارة سير عمل المحتوى لشركة **mirmaz** (براندَي mirmaz Academy و mirmaz Zone) — من إنشاء المهمة، مروراً بكتابة الكابشن والتصميم والمراجعة والموافقة، وصولاً للنشر المجدول. واجهة عربية كاملة (RTL) مع نظام صلاحيات لكل تخصص، دردشة، تقويم نشر، وتقارير.

---

## محتويات الحزمة

```
mirmaz-flow-project/
│
├── README.md                      ← هذا الملف (ابدأ هنا)
│
├── frontend/                      الواجهة
│   ├── index.html                 ← التطبيق (مربوط بـ Supabase)
│   └── supabase-client.js         ← طبقة الاتصال + الصلاحيات
│
├── database/                      قاعدة البيانات (Supabase / PostgreSQL)
│   ├── 01_schema.sql              ← الجداول (8 جداول)
│   ├── 02_permissions.sql         ← RLS + الحمايات (قابل لإعادة التشغيل)
│   ├── 03_seed.sql                ← الأقسام والقنوات فقط (صفر بيانات وهمية)
│   ├── 04_accounts.sql            ← ضبط أدوار علي وياسمين
│   └── 05_archive_import.sql      ← أرشيف 31 مهمة حقيقية (اختياري)
│
├── backend/                       الخادم
│   └── create-user-function.ts    ← دالة آمنة لإنشاء حسابات الفريق
│
├── assets/                        الهوية البصرية
│   ├── mirmaz-logo.svg            ← الشعار (قابل للتلوين)
│   ├── mirmaz-logo-blue.svg       ← أزرق
│   ├── mirmaz-logo-white.svg      ← أبيض
│   └── *.png                      ← النسخ الأصلية
│
└── docs/                          الأدلّة
    ├── دليل-الرفع.md              ← خطوات الرفع التفصيلية
    └── تقرير-الأمان.md            ← التدقيق الأمني الكامل
```

---

## وضعان للتشغيل

### وضع واحد: موقع حقيقي (Supabase + مصادقة فعلية)

الإصدار 2.0 لا يعمل بدون قاعدة بيانات — بقصد. الوضع "التجريبي" السابق
كان يحمل بيانات حقيقية وكلمات مرور بنص صريح في ملف عام.

اتبع **`docs/دليل-الرفع.md`** خطوة بخطوة. باختصار:

1. أنشئ مشروع **Supabase** (مجاني)
2. شغّل ملفات `database/` الثلاثة بالترتيب
3. أنشئ حساب السوبر أدمن
4. انشر `backend/create-user-function.ts`
5. اربط المفاتيح في `frontend/supabase-client.js`
6. ارفع على **Vercel**

النتيجة: موقع فعلي، كل شخص يدخل بحسابه ويرى ما يخصّه فقط.

---

## الميزات

**سير العمل (الخرائط):**
- كل مهمة لها خريطة دوائر تفاعلية: كتابة الكابشن ← تكست دزاين ← التصميم ← المراجعة ← الموافقة ← النشر
- المرحلة الجارية ملوّنة ونابضة، المنجزة خضراء، القادمة باهتة
- زر تعديل لكل مرحلة مع **سجل من عدّل ومتى**
- أي تعديل يعيد المسار **للتدقيق ثم موافقة الإدارة** تلقائياً

**النشر المنظّم:**
- يُفتح فقط بعد موافقة الإدارة
- يجمع الكابشن النهائي + ملف الدرايف (من المصمم) في مكان واحد
- تحديد يوم وساعة النشر
- أزرار: تأجيل / جاري النشر / تم النشر

**التقويم التفاعلي:**
- تنقّل بين الشهور، عرض منشورات كل يوم
- المنشور المكتمل يظهر بالأخضر

**المهام الشخصية:** مساحة معزولة لكل موظف (يراها هو فقط)

**الدردشة:** شات فوري عبر Supabase Realtime (الربط بتيمز لم يُنفَّذ بعد)

**أخرى:** تقارير وتحليلات، صفحة الفريق، إشعارات، بحث، مشاركة المهام، تقويم

---

## الأدوار والصلاحيات (9 تخصصات)

| التخصص | الصلاحية |
|--------|----------|
| سوبر أدمن | كل شيء + إنشاء الحسابات والأقسام |
| مانيجر | إدارة الموظفين/الأقسام + رؤية الكل |
| كوردنيتر | إنشاء المهام وبناء الخرائط |
| الإدارة | موافقة/رفض المهام |
| مدقق | مراجعة المراحل |
| كاتب كابشن / مصمم / تكست دزاينر / مونتير | العمل على مراحلهم فقط |

الصلاحيات مفروضة على **3 طبقات**: الواجهة + قاعدة البيانات (RLS) + triggers الخادم.

---

## الأمان

راجع `docs/تقرير-الأمان.md` — يتضمن تصحيحاً لادّعاءات الإصدار 1.0 غير الصحيحة.

أُغلقت في 2.0: تسريب jsonblob العام، كلمات المرور بنص صريح، تجاوز تسجيل الدخول،
تبديل الهوية، XSS، و6 ثغرات في RLS. **مُختبرة ضد PostgreSQL حقيقي: 11/11.**

⚠ **إجراءات عاجلة** إن كنت شغّلت الإصدار القديم: احذف الـblob، بدّل كل كلمة مرور،
راجع روابط الدرايف المكشوفة. التفاصيل في `docs/دليل-الرفع.md`.

---

## التقنيات

| الطبقة | التقنية |
|--------|---------|
| الواجهة | HTML/CSS/JS صِرف (بدون أي مكتبة — ملف واحد) |
| قاعدة البيانات | Supabase (PostgreSQL) |
| المصادقة | Supabase Auth (JWT + bcrypt) |
| الصلاحيات | Row Level Security (RLS) |
| الخادم | Supabase Edge Functions (Deno) |
| الاستضافة | Vercel (مقترح) |
| الخط | Cairo (عربي) |

---

## الخطوات القادمة (اختيارية)

1. **تفكيك `index.html`** (~4000 سطر) إلى وحدات ES + حذف الـ93 `onclick` المضمّن (يمنع CSP)
2. **ربط الدردشة بـ Microsoft Teams** عبر Graph API — حالياً شات Supabase عادي، ليس تيمز
3. **إضافة favicon ودومين خاص**

---

تم تطوير هذا النظام خصيصاً لـ mirmaz. كل المكوّنات جاهزة وموثّقة.
